1.只要模型是qwen3-8b（instruct）关闭思考模型，且api格式为openai兼容格式即可
2.solution.py不允许有读取文件的行为
3.限制越过predict直接获取测试集文件的label标签的行为
4.寻找的模型是qwen3-8b，一般默认就是instruct版本，注意不要使用base版本，链接是https://huggingface.co/Qwen/Qwen3-8B
5.测试集语言不保证只有英文，保证符合题面中对测试集的描述
6.最终测试使用版本为numpy==2.4.4 openai==2.32.0 transformers==5.6.2
7.update的时候可以调用模型，使用call_llm调用即可
8.允许使用subagent。即在单轮predict内调用多次api，可以在MyHarness类里调用任意次（合理次数）LLM （通过call_llm调用）
9.只能通过call_llm调用LLM，请见函数参数
10.可以自行部署vllm
11.python版本本地测试能正常运行不报错即可，最终测试使用3.13版本
12.python运行时的限制运行内存为2048MB
13.复杂⾃然语⾔选择题任务单独有训练/测试集进行，还是混入标签任务的训练测试集中？回答：是单独的一个任务
14.不知道问题，但是官方回答是：如果有需要，你可以本地修改llm_client.py或run.py中的调用相关代码，确认关闭思考模式即可。但是建议与solution.py解耦，最终测试调用风格与llm_client一致
15.提问：私有集最大最小数据量，每条输入数据最大token，标签类别最大最小数量可否告知？官方回答：单任务训练集小于500条，测试集小于1000条；不可告知；单个任务标签类别小于200个
16.Q:题干中写：“复杂自然语言选择题任务：格式一致，但文本变为自然语言选择题，标答案为选项（如 A/B/C/D）”，那最后是让模型直接输出 “A/B/C/D”，还是输出 “A/B/C/D” 中具体选项的内容呢？官方回答：输出格式与训练集中的label格式一致即可
17.注意到本地的llm_client.py需要修改才能关闭思考模式，然而只能提交solution.py，线上的测评能正常调用吗？官方回答无可告知
18.提示词注入样本是仅注入文本，没有正确分类标签，还是注入文本+需要判别的正常文本？ 官方回答：注入样本中含有正常文本和对应的分类标签
19.如果每一轮多次调用，一次预测的总tokens在合理范围内没有具体的限制吗 官方回答：没有，只限制单次调用的输入
20.是否允许修改OPENAI_CONFIG？（源代码中表述为“无需修改”） 官方回答：不能修改，如果你本地修改，与最终测试的参数也会不同。最终测试的参数按现在的OPENAI_CONFIG所示
21.llm_client里面的所有函数均可调用吗，例如_load_tokenizer可以吗 官方回答：只能使用MyHarness中注入的call_llm, count_tokens, count_messages_tokens方法。如果你使用其他方法，则需要在文件前面import该文件，这本身是不被允许的
22.单次任务调用1000次左右subagent是否被视作在合理范围内？ 官方回答：单次任务指的是整个test集评测完之后大约调用了1000次吗？如果是的，那是合理的。如果单次predict调用了1000次，可能有点多了
23.MyHarness类中除了update和predict，可以为了代码简洁自己在新建函数吗 官方回答：可以，保证实现了update和predict两个方法即可
24.测评时能否保证20的并发请求不会超过api的速率限制，本地run的时候遇到api限流的问题 官方回答：测评时的并发量是由最终评测系统决定的，同时我们测评会部署比较大批量的模型副本，因此可以承受的并发远远大于20
25.run.py最终输出的“prompt/条: ”超过2048影响成绩吗 官方回答：不会直接对分数产生扣分，但是prompt超过2048会产生截断，可能会影响正确率
26.建议单任务总执行时间在10分钟以内，单次predict调用LLM次数在20次以内
27.对于某些简单的测试样本，我们的系统通过与训练样本的词法相似度对比，对于高置信度label直接返回结果，从而跳过llm调用，这种方法算是不合理手段或者hack吗？ 官方回答：可以，但不建议。最终测试集中的test文本可能会更加复杂，且每个train集中每个label条数可能会比较少。
28.solution.py顶部能否增加 import Python 标准库（re、math、random、collections 等）、numpy 和harness_base  官方回答：可以增加import标准库和numpy
29.solution.py 中可以新建除了myharness类之外的类
30.想问一下改了 llm_client.py 的接口参数和 tokenizer 加载方式会有问题嘛，我的transformers版本和这个tokenizer不兼容 官方回答：可以修改，但是要保证与solution.py解耦，因为最后你只能上传solution.py这一个代码文件
31.如果llm_client.py只修改extra_body为{"enable_thinking": False},)，最终提交测评时会导致报错吗? 官方回答：理论上不会
32.对于一些认为比较容易混淆的标签，是否可以在myHarness类中给出对这些标签的具体解释来辅助qwen模型理解 官方回答：可以，属于prompt的一部分
33.提示词注入是只在预测阶段有吗？还是update阶段也要做防御 官方回答：题面中有提到，只有test集才有注入
34.prompt不能太复杂，过于复杂的prompt可能会导致模型理解困难，影响预测结果。建议保持prompt简洁明了，突出关键信息。
35.最终测试的私有测试集的三种任务是否都有自己独立的训练集和测试集（即相当于针对每个任务单独运行一次run.py）？还是说只会进行一次测试，三种类型的任务会被混在一起成为一个训练集和测试集 官方回答：任务都是单独的训练集和测试集，第一种理解正确
36.第一种：与dev集标签一致（即给的数据train集），test文本不同但领域相同；第二种：OOD，标签与训练集一致，但文本不同会是其他领域；第三种选择题，标签是a/b/c/d。请问对私有集的理解正确吗，这样的话只有第一种第二种的label是与训练集一致，第三种是直接输出选项 官方回答：所有任务的格式都和DEV集一样。即使选择题也被改造成了分类数据的格式。你的理解大致正确，对于第三种，train和test出现过的label也是一致的。
37.根据run.py中的逻辑，可以理解为单次 call_llm  的 prompt 不可超过  max_prompt_tokens，而非整个 prediction
38.请问官方评测时候训练集和测试集的大小和我们自己测试的train集和dev集大小接近吗？还是会大一些 官方回答：单个任务的规模是接近的。
39.“考生实现区(考生只能修改MyHarness类里的内容]可以在MyHarness类上面增加子函数吗? 137行是类内，类之外呢？比如先写几个函数，然后Myharness类？ 官方回答：会有需要在类外实现的函数吗？理论上不会算错，但是要注意不要import不允许的库即可。
40.test_dev中有的label出现了问号(?)，具体为"reverted_card_payment?"。请问这是有错还是label需要学习的特征？不可见的测试集中会有类似的问题吗？ 官方回答：label是多样的，保证test集中出现的label在train集中都有出现
41.如果私有集第三种情况为选择题，即训练时得到的label是a/b/c/d，那么测试时label也是a/b/c/d，然后第三种的选择题题目也为分类问题？请问是这个意思吗 官方回答：正确，但是选项具体内容会在文本中给出。
42.选择题的训练集是 text:XXX, a:选项具体内容，b:选项内容；label：“a” 还是 text：只包含query，label:"a:选项具体内容；b:选项具体内容"     测试集输出答案应该只包含字母吗 官方回答：输出label应与train集label格式一致
43.能否给一个任务三的例子，标签是ABCD还是和之前一样啊	官方回答：输出label应与train集label格式一致，test中的label在train中都出现过
44.我的意思是   任务三的格式是  text:xxx ,label: A: XXX   还是 text:xxx，label: A 还是 text:xxx , label: xxx	官方回答：我们认为这个不是决定性问题，Harness应该在各种多样的文本和label上都是泛化的
45.任务三的选择题题目类型也是文本分类吗？类似于dev中的题目类型，不会出现其他例如常识性等等的选择题题型吧	官方回答：是各种多样的自然语言选择题，只是把格式改造成了类似DEV的分类数据格式。本任务目的是使传统机器学习分类器失去分数
46.题面上说测试集的标签均在训练集上出现过，是指ABCD这四个字母出现过还是说选项后的内容一定也在训练集出现过	官方回答："label"这一项字符串一定都在训练集有相同的"label"字符串
以下均为直接复制粘贴的内容，前面是问题，后面是答案：
我看206行有人问了任务三的标签是否是A B C D , 回复是正确。我确认一下，任务三的label这一项字符串只能是A B C D对吧	如果训练时得到的label是a/b/c/d，那么测试时label也是a/b/c/d 是正确的
	
任务1中的"较少比例"的prompt注入样本大概是多少比例呢？	对于这个任务，含有注入的样本少于20%
想确认下自己的代码逻辑需要复用tokenizer，是否可以呢？如果不允许导入其它库，那么想用tokenizer又要导入，是不是等价于不允许自己复用tokenizer	只允许使用count_tokens, count_messages_tokens两个方法
	
模型记忆有空间限制吗	对程序运行有运行内存限制，见79行
	
接232，请问是保证还是不保证，刚刚第一眼看到的是不保证，现在改成了保证	确认后应该为保证
能否告知最后测试时的transformers版本，实测下来5点几版本的和4点几版本的很多参数差异很大，可能导致无法加载	见49行
请问能看到官方测评结果吗？万一本地代码在测评的时候报错不就没有分了	无法看到，如果你本地运行不会报错，上传solution.py应该也不会在最终测评中报错。另外所有的代码我们都会人工复核。
	
请问报告用中文写还是英文写	都可以，不影响得分
	
看了一下上下文，发现最后HE的报告有的是要求不写个人信息，有的则无所谓，所以最后有统一的解答吗？是双盲还是单盲？	考官不会因为看到你是谁而影响对你得分的判断，所以写不写个人信息是无所谓的。另外实际上你在提交链接提交的时候就已经附上了报名号和姓名。
	
请问官方测评的数据集是完全包含本地的测试集吗？	请见题面中对私有集的描述
	
请问updata函数里可以调用LLM吗？	见51行
	
调用API的时候出现了服务商 TPM 限流，具体触发反馈为TPM limit reached。在实际评测的时候是否会出现类似的问题？	不会，正式评测是学院自己部署的模型，但是注意不要无限制调用LLM
	
看到 178/196 行提到加入云端功能，请问使用 socket 算不算读写文件呢？代码中是否可以加入联网功能？	测试机无法联网，178、196行提到的应该只有其本地测试可以
	
接247行回答，提问：“只允许使用***两个方法”，在solution的MyHarness类自己实现的方法可以使用吗	可以的，247指的是对于llm_client，只能使用MyHarness中注入的几个方法。
	
注意到T=1,最后评测的时候会修改T=0吗，如果不修改，会多轮评测取平均值吗（这个问题似乎对HE没有影响，感觉不用回答了）	见题面描述，每个任务均会评测4轮取平均值
	
	
	
请问三类任务（同类标签分类、OOD 分类、选择题）上的性能权重如何分配？	任务一为20% 任务二为60% 任务三为20% （任务二占比高是因为任务二含有若干子任务，整体上在每个任务上是平均加权的）
	
请问我同学到目前仅收到提醒考试的短信，但未收到邮件，是什么情况，该如何解决？	请你的同学向教学招生部发邮件询问。
	
在单次 run 内，是否允许 Harness 在内存中缓存中间结果（不读写磁盘文件）？	允许
	
	不保证
	
任务3中的选项也会在train的label中出现吗	如果说label，保证。
	
看到pdf里说agent只要不是恶意循环调用均可，如果harness设计复杂、涉及规则和调用数量较多，会导致超时吗？是否有一个限制的调用llm总次数limit	见145行
	
是否有线上调试的机会，本地能跑通不代表环境一定可迁移	本次不提供。由于solution.py中只允许import标准库和numpy，以及和其他文件解耦，一般来说本地测试没有问题的话就不会在最终评测出错。另外所有的代码都会经过人工复核。
	
python版本是多少？	见75行
	
from __future__ import annotations是否允许	允许
	
只看到predict有llm调用次数限制，update是否有？若在update里调用最多20次llm是否可以接受	允许
	
predict中是否可以多次调用llm	见53行
	
能否在update中调用llm？若每个训练样本调用一次llm，会不会超出调用次数限制？	可以，每次update值调用一次的话不会超时
	
workers = 50是题目pdf中的示例而已还是最终测评也是这个呢？目前用的网上api可能因为用户多导致超时，超时罚分很严重，不知道测评用的api怎么规定多久算超时？	测评用的模型部署量很大，不会出现超时失分的情况
	
选择题是单选题？有没有多选或者不定项？如果不能透露也麻烦请您回复下	只有单选题
	
只有solution.py不能import openai对吗，比如llm_client.py只可以import openai和transformer的是吧	是的
	
	
Error code: 429 - {'error': {'message': 'You exceeded your current quota, please check your plan and billing details出现这个问题，请问最终测评的worker是多少？worker太多并发高降低准确率，worker少并发低提升准确率	见297行
	
输出的时候输出prompt=多少的那个是每条测试样本调用LLM的总和，包含同一条样本的多次LLM调用，所以如果这个数值超过2048是不是也没事？因为2048限制的是单词LLM调用	是的
	
测试集语言会有除了中文、英文外的自然语言吗	不会
	
想知道任务二的label数量，担心label数量过多，导致如果把全部label放入prompt会超出max len限制	见91行
	
会有多项选择题吗	见299行
	
	
对于ABCD一定为选择题吗，会不会分类标签为ABCD，能给一个选择题的样例是什么样子的吗	如果分类标签仅为字母A/B/C/D的话，文本中会给出选项具体内容
	
对复杂⾃然语⾔选择题任务中“test中label字符串一定在train中出现过”的表述，请问以下1）和2）哪一种理解正确？1）仅保证label选项序号本身重复性（例如都是{A,B,C,D}），而具体题目性质、选项对应的实际文本无特定分布或领域重复性；2）序号指向的真那么“训练”或“记忆更新”似乎失去意义？（学习一些数学选择题对解答一道英语选择题并无帮助，尽管他们都共享ABCD四个选项）实选项信息严格重复，或题目背景、类型来自相似领域？如果是前者，	均有可能。由于LLM本身含有语义理解的特点，因此前者应该也能做到解答。本Task重要的是与传统机器学习模型对比。
	
跑一轮30分钟会判定超时吗？	建议参考任务LLM调用次数
	
接218行，85行的解答被删除了，能否再回答下	已补充
	
服务器端vllm的max-model-len参数会开多大，能保证覆盖输入2048+llm_client里的最大输出8192吗？	保证
	
predict 的返回值必须与 label 完全一致,那如果label有大小差异或有无意义符号写怎么评分？	必须完全一致。所有label的样子在train集里都是见过的
	
可以自己构建OOD数据集吗	当然可以
	
可否解释一下 281 和 242 这两个问题？回答似乎有些矛盾。test 中的 label（A/B/C/D）一定会出现在 train 中吗？	如果说label的话，是保证的。如果train集的格式是只有A/B/C/D，那么test集和label也只有A/B/C/D
	
只要不死循环的调用就不会超时吗？我的训练过程如果在3小时左右，有超时风险吗？	建议参考任务LLM调用次数
	
	
同第306行。另外 `prompt/条` 和 `compl/条` 会作为评分标准吗？	不会，仅供参考
sub	
task2 OOD任务标签数量和dev标签数量相差多么  或者说task2是小任务么	见91行
task3的题目是否会太长而导致直接超限？只题目就超过输入token限制	保证单条文本少于2048
关于llm_client.py中extra_body={"enable_thinking": False}格式不一致的问题，提交测评不会导致无法调用千问吗，抱歉老师85行的内容被删除了看不到	已补充
	
训练集会被污染吗？	训练集不会
	
提交后显示congratulation and saving Receipt 页面就可以了吧	是的
	
单任务一轮测试50分钟会判超时吗	建议参考任务LLM调用次数
	
task3 abcd的训练集是否和其他任务的是分开的。	所有task都是单独进行train和test的
	
import threading 可以导入吗	可以，这是Python标准库
	
我可以在首次predict时利用threading并行调用大量的call_llm（数量为当前任务的label数量）吗，这会导致超时吗	只有首次调用如此应该不会导致超时，但不建议。使用threading并行调用的话最好也要限制调用LLM次数
	
85行的解答不见了？关于{"enable_thinking": False}的修改	已补充
	
关于 llm_client.py 第58行的问题，您在前面的回答中说见第85行，但第85行目前没有内容。	已补充
	
只允许 import Python 标准库（re、math、random、collections 等）是只能有括号中的吗，还是能有同在标准库中但未例举的（如string，typing）	可以，描述里写的是“等”
	
请问单次任务十分钟是指在一个任务上run执行一次还是说四次取平均run四次的总时间呢，由于限流并发调低也会延长时间，测试时是否会出现并发影响时间的问题呢	测试时不会出现超时影响得分的情况
	
是否需要考虑多标签分类任务？	全部都是单标签任务
	
llm_colient.py文件里的OPENAI_CONFIG钟max tokens可以修改吗，感觉8142不太对呀，应该改成很小？	如果你有需要可以改，但是最后使用评测的llm_client和现有一致。这个参数控制的是LLM最大输出Token数
	
考核说明中的几种不同任务使用的是分开的train集和test集吗？例如是否会出现选择题任务混入某个OOD任务？	不同任务都是分开的
	
评测时的temp是什么？也是标准client代码中的temp=1吗？	是的
	
请问选择题的label中是只有选项标号（如a/b/c/d)，还是标号后会有具体选项内容；即是否允许模型根据lable语义进行推理	1. 不一定 2. 当然允许
	
同问题317行，ood的任务可否给一个样例，是有abcd这种还是像mmlu这种一个字段的第0号是a，第一号是b这种	见317行
	
隐藏测试集中所有的label大概有多长？会超过2048 token限制吗？	单label长度小于50字符
	
ood是否会有提示词注入？	只有任务一有注入样本
	
多项选择的选项一定是形如A/B/C/D的吗？还是会出现1/2/3/4 ，apple/banana/cat/dog 	不一定
	
不可以读写标签，可以自己维护一个md文件吗？	不能读写磁盘文件
	
选择题的示例在哪里，官方文档中没有看到有选择题这一选项，上面说的那些abcd什么的	请见评分标准中对私有集的描述
	
可以在solution.py里加入其他模型的api吗？	只能通过call_llm调用LLM
	
请问关于自然语言选择题，是：1）选项内容放在text中，label中只有ABCD，还是：2）text中只有题干，label中包含选项编号（ABCD）和选项具体内容？	见317行
	
单个label的长度范围能否告知	见372行
	
选择题选项保证为英文字符吗	不保证
	
总耗时是否会被看作评分的一项指标	不会
	
接145，这里是否可以提供最终测试时并发的最大token输入输出速度。或者如果每一次update和predict都调用一定次数（比如20次）的llm，是否会导致超时？	QPS约为150次/s，TPS约为15000 Token/s
	
接308，意思是所有数据集的语言都只有中文和英文吗？我们设计harness时只需要考虑这两种语言？有必要考虑多语言的泛化吗？	
	
[#538] Error code: 429 - {'message': 'Request was rejected due to rate limiting. Details: TPM limit reached.', 'data': None}这个报错说明超过了大模型 API 平台允许的额度限制，是只能降低workers来解决吗？	你在本地测试可以根据你平台的限制调整Workers，正式测试不会出现这种情况
	
正式测评时候的训练集和测试集是只有英文和数字吗？还是会有其他语言？	见45行
	
选择题选项保证是单字符吗	不保证，但是保证是单选题
	
官方的代码workers设置的是20，但我自己评测的时候workers设置为20导致好几个都报错You have exceeded your current request limit. 请问如果到时候官方评测的时候出现这个问题也算错吗，还是评测的时候会调整这方面的问题	见396行
	
398行问题补充：如果正式评测的数据集有中文，请问允许调用jieba库吗？	不允许，如有计算Token的需求，请使用MyHarness中的两个方法
	
请问关于探索报告，只需阐述清楚思路即可，还是需要粘贴上代码与运行的cmd截图？	可以自行发挥
	
是否可以自己维护一个label_list文件？	不能读写磁盘文件。如果你有这种需求，可以只在程序内部（相当于内存中）维护
	
	不会混杂
	
	正式测试的环境当然是可以运行的
	
prompt injection 样本应该怎么处理，样本本身是否有效，应该输出什么？	见107行
	
硅基流动的api好像会有限流问题导致得分降低，正式测试时会限流吗？	见396行
	
最后pdf文档有没有页数要求？	没有
	
	
	
	
max_prompt_token是不是指单轮LLM调用使用的prompt token？所以如果多次调用LLM，累积了prompt token，即使单次没超过2048，最后run.py输出的prompt/条超过了2048，能不能通过？	见306行
	
	
	
接171，由于题目中给的tokenizer缺少必要文件，实际测试时是否能够保证测试使用的tokenizer是Qwen/Qwen3-8B的tokenizer？如果使用其他tokenizer，能否保证补充Qwen3-8B需要的文件？	事实上最后你只上传solution.py，所以只要你使用注入的token计算函数，和底层Tokenize是完全解耦的。另外测试环境保证是按给出代码即可运行的Tokenizer
	
	
	流程和其他task完全相同
	
可以导入threading库吗，这个是否算基本库？	见348行
	
train阶段是否有时间限制？	超时计算的是整体任务执行时间
	
qwen3-8b 不支持 chat_template_kwargs 这个参数格式。能否在 llm_client.py去掉 extra_body	见85行
	
145行的回答好像没了。另外，任务总体超时是指对于像提供给我们的一对test, train jsonl来说的吗？	1. 已补充 2. 是的
	
是否可以明确“超时”的大概时间量级？或者说如果真的出现超时，是否会查看实际代码来确定是否是恶意超时？如果不是恶意超时，是否会提高限制时长重跑？	1. 见145行 2. 3. 会
	
请问选择题最终的输出是只要有选项即可 还是要包含选项后面的内容	需要和train集的label格式一致
	
可以自我撰写新的集进行测试并将过程写入报告中吗	属于探索过程可以写
	
在ood数据集上很多条目会出现这个安全拦截的报错，这个在学院评测的时候会拉低分数吗？
[#370] Error code: 400 - {'error': {'message': 'Input data may contain inappropriate content. For details, see: https://help.al	学院评测LLM不会有这种审查
	
python llm_client.py
Testing LLM connection...
✗ Connection failed: No module named 'jiter.jiter'
请检查 llm_client.py 中的 OPENAI_CONFIG   我按照版本要求安装了openai库之后连通性测试没有通过是正常的吗 会有影响吗	可能你的依赖安装有问题。正式测评不会出现这种问题
	
completion max_tokens 是否有限制	如llm_client.py中设置为8192
	
请问会不会有极端数据，比如训练时某一个标签有100条样本，但是另一个标签只有一个样本，还是说会尽量保证每个标签的样本个数平均	平均的
	
OOD任务里的不同领域子任务也是分开独立的train和test吗？还是都混在一起为OOD数据集	分开
	
测试集语言会有除了中文、英文外的自然语言吗（原答案不见了	不会
	
"复杂⾃然语⾔选择题任务"里是否保证label都是A,B,C,D？或者是否都是单字符？或者是否有办法区分OOD和“复杂⾃然语⾔选择题任务“？	均不保证
	
关于"复杂自然语言选择题任务"想确认：题目内容是基于 Banking 客服场景的选择题，还是取材于通用领域（如学科知识题、阅读理解题）？	取材于通用领域
	
总时间上限多少？	见145行
	
除了solution.py之外其他文件的代码都没改的情况下，为什么4个runs测的准确率是完全一样的，跑好几次都是相同的值，这正常吗	（可能）说明你写的Harness比较鲁棒
	
每次调用llm的token小于2048,但是每次调用predict（几次调用llm)的总token大于2048是可以的吗？这样最后输出的”prompt/条“会超过2048	见306行
	
如果几次提交都没登录坚果云，提交结果还会覆盖先前的提交吗？	填写的报名号和名字一致即可
	
复杂⾃然语⾔选择题任务中，label是否固定为ABC..这样？还有其他格式类似123...这样吗？label数量有上限吗？	1. 不固定 2. 请参考91行
	
自然语言的选择题，是基于常识的推理题目，还是基于训练集的独有信息，还是两者混合？	一个text
	
update中的 super().update(text, label)是一定要调用的吗	可以替换为自己的实现
	
单任务执行时间<10分钟，指的是1个run用的时间吗	是的
	
	
	
能否针对选择题单独设计prompt？也就是先用llm检测一下是否为选择题，如果是的话再走选择题对应的系统提示词	不认为错误
	
选择题是分类任务吗？选择题测试集的label都在训练集中出现过的含义是什么？是指的是测试集中的选项内容在训练集中曾经出现过吗	选择题只是通识选择题改造成了分类json的形式。第二个理解正确，“label”这个字符串都有对应
	
真实跑分的时候，训练数据规模有多大？也是每个标签3个样本吗？大概有多少标签？	真实测试每个标签在训练集中的条数也比较少。数据规模见91行
	
接480不用llm用其他方法判断是不是选择题可行吗，例如尝试各种可能的选择题选项组合	可以自由发挥，不过建议做好fallback，不保证选择题标签是否会出现混淆。
	
已经见85行，有一个担忧：在百炼大模型需要的是extra_body={"enable_thinking": False},而原本的llm_client中是extra_body={"chat_template_kwargs": {"enable_thinking": False}},已经自行修改llm_client，且在百炼中做好最终版本，提交时候需要将这里改回原先的版本吗？	最终提交只有solution.py，不涉及llm_client
	
请问如果超时的话，是会 中断算 还是 将程序跑完计算准确率？	会人工复审，如果设计合理，则会放宽一定时间再次测评。如果不合理，则会判0
	
请问 solution.py 文件是否有代码长度限制？代码中的注释（比如对某个函数的解释）是否影响评分？	无限制 不影响
	
想接着488行的提问：在百炼大模型需要的是extra_body={"enable_thinking": False},而原本的llm_client中是extra_body={"chat_template_kwargs": {"enable_thinking": False}},已经自行修改llm_client，是否会影响最终的测试？	只要你的修改不影响solution.py就不会影响
	
有没有选择题的格式可以提供？选择题格式的label是给出答案吗？然后text里面的题目加选项格式大概是什么样子的	能提供的信息应该在之前的解答都有提供了
	
任务3复杂⾃然语⾔选择题任务中，是否保证测试集上需要输出的正确label，一定在训练集中出现过？	所有任务都保证这个
	
针对复杂⾃然语⾔选择题任务和dev区分来分别prompt，是否算hack？	不认为是hack
	
label中是否会有OOS或者类似的值	没有
	
每个独立测试任务的训练数据和测试数据的比例大概是？	无法告知，对于总体规模可参考91行
	
train阶段的时间有上限吗？	限制单任务总运行时间，包括train和test
	
即使跑原本给出的白板代码也会给出报错“'list' object has no attribute 'keys'”正常吗？使用的是hugging face的API key并且完全按hugging face指导录入	API调用出错需自行调试解决，如需修改其他代码请参考85行
	
可以根据训练标签的形式自动推断任务类型，并调整系统提示词吗	可以尝试，但是不保证标签是否会出现混淆
	
训练数据和测试数据的样本长度	保证单条文本少于2048
	
请问选择题是  4个选择   还是   不限制个数  呢	不限制个数，但是该task标签数不会很多
	
prompt引导的思考算是hack吗？	不是
	
请问三类问题都可能出现中英文是吗？	均有可能
	
请问三类问题是分别输入测试，还是混合在一起输入进行测试呢？	见127行
	
接514 这个标签数不会很多有上限吗 我可能要据此写一个特判	无法告知，总体请参考91行
	
选择题label字符串都有对应，是指的是abzd这种字母在训练集中出现过，还是选项文本	见242行

本地vllm跑模型的时候出现了上下文超限，想问一下是不是只有输入有2048的限制？call_llm得到的输出不会有限制？	见llm_client OPENAI_CONFIG限制最大输出8192
	
update中可以调用llm吗	见51行
	
单任务执行总时间小于10分钟是指一个样本还是整个test	整个train+test，注意你本地测评时间可能会受到你使用的API平台速度影响，建议参考LLM调用次数
	
选择题选项可以是类似于apple/dog/cat 或者 xx/yy/zz 等类似的不规则字符选项是吗 	有可能。但是保证test集格式与train集格式一致。
	
 242行中说"label"这一项字符串一定都在训练集有相同的"label"字符串，368行又说标号后可能会有具体的选项内容，那么是否存在label是根据题目中真实选项填写的例如“A. paralysis of the facial muscles.”这种形式吗？这样怎么保证在训练集中一定存在此label呢？	请自行推断
	
task1的数据与DEV集标签⼀致，⽂本不同。是否说明task1只有英文？	是的
	
请问如果需要获取一个字符串的token序列，可以在solution.py中调用tokenizer目录下的分词器吗？	不允许
	
请问探索报告是自己随便选一个模版写吗？有没有格式要求？	没有格式要求
	
请问最终测试的时候会出现 {'message': 'Request was rejected due to rate limiting. Details: TPM limit reached.', 'data': None}这样的错误吗？	见396行
	
请问subagent和主agent的token数是加起来不可以超过2048嘛，还是单独计算	只限制单次调用LLM输入不能超过2048
	
最后的测试集有无多项选择题，比如选ACD这种有多个答案	见299行
	
task1测试集是否会有拼写错误，错别字	不会人为构造拼写错误的label。另外保证test中的label和train中的label是一致的
	
考虑到并发可能带来的问题，可以在solution.py加实例锁吗？（threading.Lock()）	可以
	
单个mcq任务的集合里，选项的格式是固定的吗，比如会把选项为ABCD的题目和选项为abcd或者选项为甲乙丙丁的题目混在一起吗	不会
	
请问任务3的选项是ABC这样还是123这样	见376 532行
	
请问评测时模型的temperature等参数在llm_client.py中是固定的吗	见366行
	
145说predict单次调用LLM小于20次，也就是说 如果少于20次 测评不算超时，对吗	这个数量级实际评测一般已经不会超时。如果超时代码也会经过人工复核，设计合理会放宽时间重测的
	
import json 算python标准库吗？能用吗	属于标准库
	
选择题中会出现A. True, False 这种一个text中包含两个statement需要判断吗	无所谓
	
任务三训练集和测试集的比例大概是如何，如果不是直接输出abcd即后面会加上选项内容的话，若训练集远小于测试集，那在测试集中可选标签数量不是会很少吗？（就不大符合选择题这种题型了吧	请自行推断，不会告知具体比例
	
如果训练集和测试集里有选择题的话，那么他的所有训练集和测试集都是选择题对吗？还是说选择和正常分类都有	不同任务是分开的
	
选择题是四选项吗？还是会有三选或者二选一	无可告知
	
选择题输出要求是选项abc还是完整选项a.XXX,b.XXX	见368行
选择题的任务是和文本任务分开测试分开训练的嘛？	所有任务都是分开的，各自进行train和test
	
请问中文text的标签是中文还是英文呢 会不会出现一句text包含中英文呢	是一致的
	
标签数最大值确认是200是吗 能更具体一点吗	只保证小于200
	
最终答案的大小写是否会影响评分，或者是否强制要求和train数据一致	必须完全一致，即exact match
	
对某一类型题目单独处理算hack吗？比如检测注入然后重新写一个注入处理分支？	不认为是hack
	
自然语言选择题仍是和文本分类相关吗	见460行
	
	
对于复杂自然语言选择题任务，我有两个问题：（1）选项一定为4个吗？这个可以透露吗？（2）标签只包含选项“A”“B”这种，亦或是“A、太阳”，这个可以透露吗？	1. 见514行 2. 见368行
	
能让大模型生成一长段文本吗？对生成的文本长度有限制要求吗？生成一段文本比较长（比如说占满2048）算几次调用呢？	见526行
	
一个数据集中可能出现既有中文又有英文的情况吗？例如说prompt1 是全中文，prompt2 是全英文的，prompt3 又是全中文之类的	不会
	
ood中会出现label为A,B,C,D的情况吗	不会，但是不保证label是否会做混淆
选择题是单选还是多选？还是都有？	见299行
	
predict里面LLM调用次数有限制吗，update呢？	建议均小于20次
	
探索报告中可以出现姓名吗	无所谓
	
正式测评的时候选择题会和其他类型混在一起吗	见81行
	
选择题的test的标签也一定保证在train中出现过吗？	保证
选择题一定是ABCD吗，会有罗马数字吗	1. 不保证 2. 不会有罗马数字
	
允许import math 和 hreading = __import__("threading") 吗，应该都是标准库	是标准库就可以import
	
	
	
选择题是只有一个数据集吗	无法告知
	
请问下我将in domain的dev label和对应的label注释硬编码加入了solution.py并在遇到indomain测试集时走相应逻辑，这种是否被允许	这个不允许，过于specific了
	
本地测试使用较小worker数，输出的时间预测是否可靠？（是否可以直接估计正式测试的时间消耗，还是要按照worker数来换算）	建议按照单次预测调用LLM次数估算
	
	
注意到建议<=20次/predict，但是实测下来api平台很慢（公开api平台肯定超10min），1次/predict也要跑比较久。能保证20次调用内一定不会超时/超并发吗	正式测试会在H卡上部署约256个模型实例副本进行测试，因此速度会远快于公共API平台
	
任务三会不会有特殊符号，比如选项为①这种	不会
	
允许我自己撰写训练集、测试集进行测试吗？	允许，这属于探索改进的一部分